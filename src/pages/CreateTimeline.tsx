function CreateTimeline() {
    // TODO: onSubmit -> create timeline in DB, then navigate to /timelines or /timelines/:id
    return (
        <div>
            <h2>Create a New Timeline</h2>
            <form>
                {/* fields */}
                <button type="submit">Save</button>
            </form>
        </div>
    );
}

export default CreateTimeline;
